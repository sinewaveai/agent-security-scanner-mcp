{
  "hooks": {
    "post-tool-use": [
      {
        "matcher": "Write|Edit",
        "command": "python3 src/analyzer.py \"$FILE_PATH\" 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); print(f'⚠️ {len(d)} security issues detected. Run /fix-security to auto-fix.' if isinstance(d,list) and len(d)>0 else '')\" 2>/dev/null || true"
      }
    ]
  }
}
